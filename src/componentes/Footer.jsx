import React from "react";

const Footer = () => {
    return(
        <footer>
            <p>Esta es una galeria de imagenes creada con React.</p>
            <button className="btn btn-dark">Ver mas</button>
        </footer>
    );
};

export default Footer;